# Fuse

Fuse is an agent harness that uses a vision-capable language model to observe a computer GUI and perform actions on it through defined operating-system tools.

It captures the current screen, asks the model for the next action, and executes one tool call at a time.
